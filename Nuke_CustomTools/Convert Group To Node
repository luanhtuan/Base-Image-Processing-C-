GNames = []
TargetNodes = []
for G in nuke.allNodes('Group'):
    GNames.append(G)
    GN = str(len(GNames))
for sel in range(0, int(GN)):
   G = GNames[(sel)]
   G.setSelected(True)
for selA in nuke.selectedNodes():
   nuke.selectAll()
   nuke.invertSelection()
   selA.expand()
